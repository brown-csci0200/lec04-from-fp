package src;

// Name of a type
public interface IAnimal {

    // In the interface, we can write the names of methods we want
    // "all Animals" (ie, classes that implement IAnimal) to have
    // We just write this like the first line of the method, ("signature")
    public boolean isNormalSize();


}
